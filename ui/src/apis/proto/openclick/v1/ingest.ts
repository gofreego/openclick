// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: proto/openclick/v1/ingest.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Struct } from "../../../google/protobuf/struct";
import { Timestamp } from "../../../google/protobuf/timestamp";

export const protobufPackage = "v1";

export interface RegisterDeviceRequest {
  apiKey?:
    | string
    | undefined;
  /** device properties: $browser, $os, $screen_*, $lib, etc. */
  properties: { [key: string]: any } | undefined;
}

export interface RegisterDeviceResponse {
  deviceId: string;
}

export interface CaptureEventRequest {
  apiKey?: string | undefined;
  event: string;
  distinctId: string;
  timestamp?: Date | undefined;
  properties: { [key: string]: any } | undefined;
  sessionId?: string | undefined;
  deviceId?: string | undefined;
}

export interface CaptureEventResponse {
  status: number;
}

export interface BatchEvent {
  event: string;
  distinctId: string;
  timestamp?: Date | undefined;
  properties: { [key: string]: any } | undefined;
  sessionId?: string | undefined;
  deviceId?: string | undefined;
}

export interface BatchCaptureRequest {
  apiKey?: string | undefined;
  batch: BatchEvent[];
}

export interface BatchCaptureResponse {
  status: number;
  count: number;
}

export interface IdentifyRequest {
  apiKey?: string | undefined;
  distinctId: string;
  properties: { [key: string]: any } | undefined;
}

export interface IdentifyResponse {
  status: number;
}

export interface AliasRequest {
  apiKey?: string | undefined;
  alias: string;
  distinctId: string;
}

export interface AliasResponse {
  status: number;
}

export interface IngestReplayRequest {
  apiKey?: string | undefined;
  sessionId: string;
  distinctId: string;
  chunkIndex: number;
  data: string;
  metadata: { [key: string]: string };
}

export interface IngestReplayRequest_MetadataEntry {
  key: string;
  value: string;
}

export interface IngestReplayResponse {
  status: number;
}

function createBaseRegisterDeviceRequest(): RegisterDeviceRequest {
  return { apiKey: undefined, properties: undefined };
}

export const RegisterDeviceRequest: MessageFns<RegisterDeviceRequest> = {
  encode(message: RegisterDeviceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.apiKey !== undefined) {
      writer.uint32(10).string(message.apiKey);
    }
    if (message.properties !== undefined) {
      Struct.encode(Struct.wrap(message.properties), writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterDeviceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterDeviceRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.apiKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.properties = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterDeviceRequest {
    return {
      apiKey: isSet(object.apiKey)
        ? globalThis.String(object.apiKey)
        : isSet(object.api_key)
        ? globalThis.String(object.api_key)
        : undefined,
      properties: isObject(object.properties) ? object.properties : undefined,
    };
  },

  toJSON(message: RegisterDeviceRequest): unknown {
    const obj: any = {};
    if (message.apiKey !== undefined) {
      obj.apiKey = message.apiKey;
    }
    if (message.properties !== undefined) {
      obj.properties = message.properties;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterDeviceRequest>, I>>(base?: I): RegisterDeviceRequest {
    return RegisterDeviceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterDeviceRequest>, I>>(object: I): RegisterDeviceRequest {
    const message = createBaseRegisterDeviceRequest();
    message.apiKey = object.apiKey ?? undefined;
    message.properties = object.properties ?? undefined;
    return message;
  },
};

function createBaseRegisterDeviceResponse(): RegisterDeviceResponse {
  return { deviceId: "" };
}

export const RegisterDeviceResponse: MessageFns<RegisterDeviceResponse> = {
  encode(message: RegisterDeviceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.deviceId !== "") {
      writer.uint32(10).string(message.deviceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RegisterDeviceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseRegisterDeviceResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.deviceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): RegisterDeviceResponse {
    return {
      deviceId: isSet(object.deviceId)
        ? globalThis.String(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.String(object.device_id)
        : "",
    };
  },

  toJSON(message: RegisterDeviceResponse): unknown {
    const obj: any = {};
    if (message.deviceId !== "") {
      obj.deviceId = message.deviceId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<RegisterDeviceResponse>, I>>(base?: I): RegisterDeviceResponse {
    return RegisterDeviceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RegisterDeviceResponse>, I>>(object: I): RegisterDeviceResponse {
    const message = createBaseRegisterDeviceResponse();
    message.deviceId = object.deviceId ?? "";
    return message;
  },
};

function createBaseCaptureEventRequest(): CaptureEventRequest {
  return {
    apiKey: undefined,
    event: "",
    distinctId: "",
    timestamp: undefined,
    properties: undefined,
    sessionId: undefined,
    deviceId: undefined,
  };
}

export const CaptureEventRequest: MessageFns<CaptureEventRequest> = {
  encode(message: CaptureEventRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.apiKey !== undefined) {
      writer.uint32(10).string(message.apiKey);
    }
    if (message.event !== "") {
      writer.uint32(18).string(message.event);
    }
    if (message.distinctId !== "") {
      writer.uint32(26).string(message.distinctId);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(34).fork()).join();
    }
    if (message.properties !== undefined) {
      Struct.encode(Struct.wrap(message.properties), writer.uint32(42).fork()).join();
    }
    if (message.sessionId !== undefined) {
      writer.uint32(50).string(message.sessionId);
    }
    if (message.deviceId !== undefined) {
      writer.uint32(58).string(message.deviceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CaptureEventRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCaptureEventRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.apiKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.event = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.distinctId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.properties = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 7: {
          if (tag !== 58) {
            break;
          }

          message.deviceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CaptureEventRequest {
    return {
      apiKey: isSet(object.apiKey)
        ? globalThis.String(object.apiKey)
        : isSet(object.api_key)
        ? globalThis.String(object.api_key)
        : undefined,
      event: isSet(object.event) ? globalThis.String(object.event) : "",
      distinctId: isSet(object.distinctId)
        ? globalThis.String(object.distinctId)
        : isSet(object.distinct_id)
        ? globalThis.String(object.distinct_id)
        : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      properties: isObject(object.properties) ? object.properties : undefined,
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : undefined,
      deviceId: isSet(object.deviceId)
        ? globalThis.String(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.String(object.device_id)
        : undefined,
    };
  },

  toJSON(message: CaptureEventRequest): unknown {
    const obj: any = {};
    if (message.apiKey !== undefined) {
      obj.apiKey = message.apiKey;
    }
    if (message.event !== "") {
      obj.event = message.event;
    }
    if (message.distinctId !== "") {
      obj.distinctId = message.distinctId;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.properties !== undefined) {
      obj.properties = message.properties;
    }
    if (message.sessionId !== undefined) {
      obj.sessionId = message.sessionId;
    }
    if (message.deviceId !== undefined) {
      obj.deviceId = message.deviceId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CaptureEventRequest>, I>>(base?: I): CaptureEventRequest {
    return CaptureEventRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CaptureEventRequest>, I>>(object: I): CaptureEventRequest {
    const message = createBaseCaptureEventRequest();
    message.apiKey = object.apiKey ?? undefined;
    message.event = object.event ?? "";
    message.distinctId = object.distinctId ?? "";
    message.timestamp = object.timestamp ?? undefined;
    message.properties = object.properties ?? undefined;
    message.sessionId = object.sessionId ?? undefined;
    message.deviceId = object.deviceId ?? undefined;
    return message;
  },
};

function createBaseCaptureEventResponse(): CaptureEventResponse {
  return { status: 0 };
}

export const CaptureEventResponse: MessageFns<CaptureEventResponse> = {
  encode(message: CaptureEventResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CaptureEventResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCaptureEventResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CaptureEventResponse {
    return { status: isSet(object.status) ? globalThis.Number(object.status) : 0 };
  },

  toJSON(message: CaptureEventResponse): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CaptureEventResponse>, I>>(base?: I): CaptureEventResponse {
    return CaptureEventResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CaptureEventResponse>, I>>(object: I): CaptureEventResponse {
    const message = createBaseCaptureEventResponse();
    message.status = object.status ?? 0;
    return message;
  },
};

function createBaseBatchEvent(): BatchEvent {
  return {
    event: "",
    distinctId: "",
    timestamp: undefined,
    properties: undefined,
    sessionId: undefined,
    deviceId: undefined,
  };
}

export const BatchEvent: MessageFns<BatchEvent> = {
  encode(message: BatchEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.event !== "") {
      writer.uint32(10).string(message.event);
    }
    if (message.distinctId !== "") {
      writer.uint32(18).string(message.distinctId);
    }
    if (message.timestamp !== undefined) {
      Timestamp.encode(toTimestamp(message.timestamp), writer.uint32(26).fork()).join();
    }
    if (message.properties !== undefined) {
      Struct.encode(Struct.wrap(message.properties), writer.uint32(34).fork()).join();
    }
    if (message.sessionId !== undefined) {
      writer.uint32(42).string(message.sessionId);
    }
    if (message.deviceId !== undefined) {
      writer.uint32(50).string(message.deviceId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchEvent();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.event = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.distinctId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.timestamp = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.properties = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.deviceId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchEvent {
    return {
      event: isSet(object.event) ? globalThis.String(object.event) : "",
      distinctId: isSet(object.distinctId)
        ? globalThis.String(object.distinctId)
        : isSet(object.distinct_id)
        ? globalThis.String(object.distinct_id)
        : "",
      timestamp: isSet(object.timestamp) ? fromJsonTimestamp(object.timestamp) : undefined,
      properties: isObject(object.properties) ? object.properties : undefined,
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : undefined,
      deviceId: isSet(object.deviceId)
        ? globalThis.String(object.deviceId)
        : isSet(object.device_id)
        ? globalThis.String(object.device_id)
        : undefined,
    };
  },

  toJSON(message: BatchEvent): unknown {
    const obj: any = {};
    if (message.event !== "") {
      obj.event = message.event;
    }
    if (message.distinctId !== "") {
      obj.distinctId = message.distinctId;
    }
    if (message.timestamp !== undefined) {
      obj.timestamp = message.timestamp.toISOString();
    }
    if (message.properties !== undefined) {
      obj.properties = message.properties;
    }
    if (message.sessionId !== undefined) {
      obj.sessionId = message.sessionId;
    }
    if (message.deviceId !== undefined) {
      obj.deviceId = message.deviceId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BatchEvent>, I>>(base?: I): BatchEvent {
    return BatchEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchEvent>, I>>(object: I): BatchEvent {
    const message = createBaseBatchEvent();
    message.event = object.event ?? "";
    message.distinctId = object.distinctId ?? "";
    message.timestamp = object.timestamp ?? undefined;
    message.properties = object.properties ?? undefined;
    message.sessionId = object.sessionId ?? undefined;
    message.deviceId = object.deviceId ?? undefined;
    return message;
  },
};

function createBaseBatchCaptureRequest(): BatchCaptureRequest {
  return { apiKey: undefined, batch: [] };
}

export const BatchCaptureRequest: MessageFns<BatchCaptureRequest> = {
  encode(message: BatchCaptureRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.apiKey !== undefined) {
      writer.uint32(10).string(message.apiKey);
    }
    for (const v of message.batch) {
      BatchEvent.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchCaptureRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchCaptureRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.apiKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.batch.push(BatchEvent.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchCaptureRequest {
    return {
      apiKey: isSet(object.apiKey)
        ? globalThis.String(object.apiKey)
        : isSet(object.api_key)
        ? globalThis.String(object.api_key)
        : undefined,
      batch: globalThis.Array.isArray(object?.batch) ? object.batch.map((e: any) => BatchEvent.fromJSON(e)) : [],
    };
  },

  toJSON(message: BatchCaptureRequest): unknown {
    const obj: any = {};
    if (message.apiKey !== undefined) {
      obj.apiKey = message.apiKey;
    }
    if (message.batch?.length) {
      obj.batch = message.batch.map((e) => BatchEvent.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BatchCaptureRequest>, I>>(base?: I): BatchCaptureRequest {
    return BatchCaptureRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchCaptureRequest>, I>>(object: I): BatchCaptureRequest {
    const message = createBaseBatchCaptureRequest();
    message.apiKey = object.apiKey ?? undefined;
    message.batch = object.batch?.map((e) => BatchEvent.fromPartial(e)) || [];
    return message;
  },
};

function createBaseBatchCaptureResponse(): BatchCaptureResponse {
  return { status: 0, count: 0 };
}

export const BatchCaptureResponse: MessageFns<BatchCaptureResponse> = {
  encode(message: BatchCaptureResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    if (message.count !== 0) {
      writer.uint32(16).int32(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): BatchCaptureResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseBatchCaptureResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.count = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): BatchCaptureResponse {
    return {
      status: isSet(object.status) ? globalThis.Number(object.status) : 0,
      count: isSet(object.count) ? globalThis.Number(object.count) : 0,
    };
  },

  toJSON(message: BatchCaptureResponse): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    if (message.count !== 0) {
      obj.count = Math.round(message.count);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<BatchCaptureResponse>, I>>(base?: I): BatchCaptureResponse {
    return BatchCaptureResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<BatchCaptureResponse>, I>>(object: I): BatchCaptureResponse {
    const message = createBaseBatchCaptureResponse();
    message.status = object.status ?? 0;
    message.count = object.count ?? 0;
    return message;
  },
};

function createBaseIdentifyRequest(): IdentifyRequest {
  return { apiKey: undefined, distinctId: "", properties: undefined };
}

export const IdentifyRequest: MessageFns<IdentifyRequest> = {
  encode(message: IdentifyRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.apiKey !== undefined) {
      writer.uint32(10).string(message.apiKey);
    }
    if (message.distinctId !== "") {
      writer.uint32(18).string(message.distinctId);
    }
    if (message.properties !== undefined) {
      Struct.encode(Struct.wrap(message.properties), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IdentifyRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIdentifyRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.apiKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.distinctId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.properties = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IdentifyRequest {
    return {
      apiKey: isSet(object.apiKey)
        ? globalThis.String(object.apiKey)
        : isSet(object.api_key)
        ? globalThis.String(object.api_key)
        : undefined,
      distinctId: isSet(object.distinctId)
        ? globalThis.String(object.distinctId)
        : isSet(object.distinct_id)
        ? globalThis.String(object.distinct_id)
        : "",
      properties: isObject(object.properties) ? object.properties : undefined,
    };
  },

  toJSON(message: IdentifyRequest): unknown {
    const obj: any = {};
    if (message.apiKey !== undefined) {
      obj.apiKey = message.apiKey;
    }
    if (message.distinctId !== "") {
      obj.distinctId = message.distinctId;
    }
    if (message.properties !== undefined) {
      obj.properties = message.properties;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IdentifyRequest>, I>>(base?: I): IdentifyRequest {
    return IdentifyRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IdentifyRequest>, I>>(object: I): IdentifyRequest {
    const message = createBaseIdentifyRequest();
    message.apiKey = object.apiKey ?? undefined;
    message.distinctId = object.distinctId ?? "";
    message.properties = object.properties ?? undefined;
    return message;
  },
};

function createBaseIdentifyResponse(): IdentifyResponse {
  return { status: 0 };
}

export const IdentifyResponse: MessageFns<IdentifyResponse> = {
  encode(message: IdentifyResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IdentifyResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIdentifyResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IdentifyResponse {
    return { status: isSet(object.status) ? globalThis.Number(object.status) : 0 };
  },

  toJSON(message: IdentifyResponse): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IdentifyResponse>, I>>(base?: I): IdentifyResponse {
    return IdentifyResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IdentifyResponse>, I>>(object: I): IdentifyResponse {
    const message = createBaseIdentifyResponse();
    message.status = object.status ?? 0;
    return message;
  },
};

function createBaseAliasRequest(): AliasRequest {
  return { apiKey: undefined, alias: "", distinctId: "" };
}

export const AliasRequest: MessageFns<AliasRequest> = {
  encode(message: AliasRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.apiKey !== undefined) {
      writer.uint32(10).string(message.apiKey);
    }
    if (message.alias !== "") {
      writer.uint32(18).string(message.alias);
    }
    if (message.distinctId !== "") {
      writer.uint32(26).string(message.distinctId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AliasRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAliasRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.apiKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.alias = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.distinctId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AliasRequest {
    return {
      apiKey: isSet(object.apiKey)
        ? globalThis.String(object.apiKey)
        : isSet(object.api_key)
        ? globalThis.String(object.api_key)
        : undefined,
      alias: isSet(object.alias) ? globalThis.String(object.alias) : "",
      distinctId: isSet(object.distinctId)
        ? globalThis.String(object.distinctId)
        : isSet(object.distinct_id)
        ? globalThis.String(object.distinct_id)
        : "",
    };
  },

  toJSON(message: AliasRequest): unknown {
    const obj: any = {};
    if (message.apiKey !== undefined) {
      obj.apiKey = message.apiKey;
    }
    if (message.alias !== "") {
      obj.alias = message.alias;
    }
    if (message.distinctId !== "") {
      obj.distinctId = message.distinctId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AliasRequest>, I>>(base?: I): AliasRequest {
    return AliasRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AliasRequest>, I>>(object: I): AliasRequest {
    const message = createBaseAliasRequest();
    message.apiKey = object.apiKey ?? undefined;
    message.alias = object.alias ?? "";
    message.distinctId = object.distinctId ?? "";
    return message;
  },
};

function createBaseAliasResponse(): AliasResponse {
  return { status: 0 };
}

export const AliasResponse: MessageFns<AliasResponse> = {
  encode(message: AliasResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): AliasResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAliasResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): AliasResponse {
    return { status: isSet(object.status) ? globalThis.Number(object.status) : 0 };
  },

  toJSON(message: AliasResponse): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<AliasResponse>, I>>(base?: I): AliasResponse {
    return AliasResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<AliasResponse>, I>>(object: I): AliasResponse {
    const message = createBaseAliasResponse();
    message.status = object.status ?? 0;
    return message;
  },
};

function createBaseIngestReplayRequest(): IngestReplayRequest {
  return { apiKey: undefined, sessionId: "", distinctId: "", chunkIndex: 0, data: "", metadata: {} };
}

export const IngestReplayRequest: MessageFns<IngestReplayRequest> = {
  encode(message: IngestReplayRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.apiKey !== undefined) {
      writer.uint32(10).string(message.apiKey);
    }
    if (message.sessionId !== "") {
      writer.uint32(18).string(message.sessionId);
    }
    if (message.distinctId !== "") {
      writer.uint32(26).string(message.distinctId);
    }
    if (message.chunkIndex !== 0) {
      writer.uint32(32).int32(message.chunkIndex);
    }
    if (message.data !== "") {
      writer.uint32(42).string(message.data);
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      IngestReplayRequest_MetadataEntry.encode({ key: key as any, value }, writer.uint32(50).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IngestReplayRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIngestReplayRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.apiKey = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.sessionId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.distinctId = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.chunkIndex = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.data = reader.string();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = IngestReplayRequest_MetadataEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.metadata[entry6.key] = entry6.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IngestReplayRequest {
    return {
      apiKey: isSet(object.apiKey)
        ? globalThis.String(object.apiKey)
        : isSet(object.api_key)
        ? globalThis.String(object.api_key)
        : undefined,
      sessionId: isSet(object.sessionId)
        ? globalThis.String(object.sessionId)
        : isSet(object.session_id)
        ? globalThis.String(object.session_id)
        : "",
      distinctId: isSet(object.distinctId)
        ? globalThis.String(object.distinctId)
        : isSet(object.distinct_id)
        ? globalThis.String(object.distinct_id)
        : "",
      chunkIndex: isSet(object.chunkIndex)
        ? globalThis.Number(object.chunkIndex)
        : isSet(object.chunk_index)
        ? globalThis.Number(object.chunk_index)
        : 0,
      data: isSet(object.data) ? globalThis.String(object.data) : "",
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: IngestReplayRequest): unknown {
    const obj: any = {};
    if (message.apiKey !== undefined) {
      obj.apiKey = message.apiKey;
    }
    if (message.sessionId !== "") {
      obj.sessionId = message.sessionId;
    }
    if (message.distinctId !== "") {
      obj.distinctId = message.distinctId;
    }
    if (message.chunkIndex !== 0) {
      obj.chunkIndex = Math.round(message.chunkIndex);
    }
    if (message.data !== "") {
      obj.data = message.data;
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IngestReplayRequest>, I>>(base?: I): IngestReplayRequest {
    return IngestReplayRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IngestReplayRequest>, I>>(object: I): IngestReplayRequest {
    const message = createBaseIngestReplayRequest();
    message.apiKey = object.apiKey ?? undefined;
    message.sessionId = object.sessionId ?? "";
    message.distinctId = object.distinctId ?? "";
    message.chunkIndex = object.chunkIndex ?? 0;
    message.data = object.data ?? "";
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseIngestReplayRequest_MetadataEntry(): IngestReplayRequest_MetadataEntry {
  return { key: "", value: "" };
}

export const IngestReplayRequest_MetadataEntry: MessageFns<IngestReplayRequest_MetadataEntry> = {
  encode(message: IngestReplayRequest_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IngestReplayRequest_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIngestReplayRequest_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IngestReplayRequest_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: IngestReplayRequest_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IngestReplayRequest_MetadataEntry>, I>>(
    base?: I,
  ): IngestReplayRequest_MetadataEntry {
    return IngestReplayRequest_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IngestReplayRequest_MetadataEntry>, I>>(
    object: I,
  ): IngestReplayRequest_MetadataEntry {
    const message = createBaseIngestReplayRequest_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseIngestReplayResponse(): IngestReplayResponse {
  return { status: 0 };
}

export const IngestReplayResponse: MessageFns<IngestReplayResponse> = {
  encode(message: IngestReplayResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.status !== 0) {
      writer.uint32(8).int32(message.status);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IngestReplayResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseIngestReplayResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.status = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): IngestReplayResponse {
    return { status: isSet(object.status) ? globalThis.Number(object.status) : 0 };
  },

  toJSON(message: IngestReplayResponse): unknown {
    const obj: any = {};
    if (message.status !== 0) {
      obj.status = Math.round(message.status);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<IngestReplayResponse>, I>>(base?: I): IngestReplayResponse {
    return IngestReplayResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IngestReplayResponse>, I>>(object: I): IngestReplayResponse {
    const message = createBaseIngestReplayResponse();
    message.status = object.status ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
