apiVersion: v1
kind: Service
metadata:
  name: openclick
spec:
  selector:
    app: openclick
  ports:
    - name: http
      port: 9093
      targetPort: http
    - name: grpc
      port: 9094
      targetPort: grpc
