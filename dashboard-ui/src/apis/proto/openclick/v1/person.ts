// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.8
//   protoc               unknown
// source: proto/openclick/v1/person.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Struct } from "../../../google/protobuf/struct";
import { Timestamp } from "../../../google/protobuf/timestamp";
import { EventResult } from "./analytics";

export const protobufPackage = "v1";

export interface PersonResponse {
  id: string;
  distinctId: string;
  properties: { [key: string]: any } | undefined;
  createdAt: Date | undefined;
}

export interface ListPersonsRequest {
  projectId: string;
  search?: string | undefined;
  limit?: number | undefined;
  offset?: number | undefined;
}

export interface ListPersonsResponse {
  results: PersonResponse[];
  total: string;
}

export interface GetPersonRequest {
  projectId: string;
  distinctId: string;
}

export interface GetPersonResponse {
  person: PersonResponse | undefined;
  recentEvents: EventResult[];
}

export interface DeletePersonRequest {
  projectId: string;
  distinctId: string;
}

export interface DeletePersonResponse {
}

export interface CohortResponse {
  id: string;
  name: string;
  filters: { [key: string]: any } | undefined;
  personCount: string;
  createdAt: Date | undefined;
}

export interface ListCohortsRequest {
  projectId: string;
}

export interface ListCohortsResponse {
  results: CohortResponse[];
}

export interface CreateCohortRequest {
  projectId: string;
  name: string;
  filters: { [key: string]: any } | undefined;
}

export interface DeleteCohortRequest {
  projectId: string;
  cohortId: string;
}

export interface DeleteCohortResponse {
}

function createBasePersonResponse(): PersonResponse {
  return { id: "", distinctId: "", properties: undefined, createdAt: undefined };
}

export const PersonResponse: MessageFns<PersonResponse> = {
  encode(message: PersonResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.distinctId !== "") {
      writer.uint32(18).string(message.distinctId);
    }
    if (message.properties !== undefined) {
      Struct.encode(Struct.wrap(message.properties), writer.uint32(26).fork()).join();
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PersonResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePersonResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.distinctId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.properties = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PersonResponse {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      distinctId: isSet(object.distinctId)
        ? globalThis.String(object.distinctId)
        : isSet(object.distinct_id)
        ? globalThis.String(object.distinct_id)
        : "",
      properties: isObject(object.properties) ? object.properties : undefined,
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
    };
  },

  toJSON(message: PersonResponse): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.distinctId !== "") {
      obj.distinctId = message.distinctId;
    }
    if (message.properties !== undefined) {
      obj.properties = message.properties;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PersonResponse>, I>>(base?: I): PersonResponse {
    return PersonResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PersonResponse>, I>>(object: I): PersonResponse {
    const message = createBasePersonResponse();
    message.id = object.id ?? "";
    message.distinctId = object.distinctId ?? "";
    message.properties = object.properties ?? undefined;
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

function createBaseListPersonsRequest(): ListPersonsRequest {
  return { projectId: "", search: undefined, limit: undefined, offset: undefined };
}

export const ListPersonsRequest: MessageFns<ListPersonsRequest> = {
  encode(message: ListPersonsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.projectId !== "") {
      writer.uint32(10).string(message.projectId);
    }
    if (message.search !== undefined) {
      writer.uint32(18).string(message.search);
    }
    if (message.limit !== undefined) {
      writer.uint32(24).int32(message.limit);
    }
    if (message.offset !== undefined) {
      writer.uint32(32).int32(message.offset);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPersonsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPersonsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.projectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.search = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.limit = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.offset = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListPersonsRequest {
    return {
      projectId: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
      search: isSet(object.search) ? globalThis.String(object.search) : undefined,
      limit: isSet(object.limit) ? globalThis.Number(object.limit) : undefined,
      offset: isSet(object.offset) ? globalThis.Number(object.offset) : undefined,
    };
  },

  toJSON(message: ListPersonsRequest): unknown {
    const obj: any = {};
    if (message.projectId !== "") {
      obj.projectId = message.projectId;
    }
    if (message.search !== undefined) {
      obj.search = message.search;
    }
    if (message.limit !== undefined) {
      obj.limit = Math.round(message.limit);
    }
    if (message.offset !== undefined) {
      obj.offset = Math.round(message.offset);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListPersonsRequest>, I>>(base?: I): ListPersonsRequest {
    return ListPersonsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListPersonsRequest>, I>>(object: I): ListPersonsRequest {
    const message = createBaseListPersonsRequest();
    message.projectId = object.projectId ?? "";
    message.search = object.search ?? undefined;
    message.limit = object.limit ?? undefined;
    message.offset = object.offset ?? undefined;
    return message;
  },
};

function createBaseListPersonsResponse(): ListPersonsResponse {
  return { results: [], total: "0" };
}

export const ListPersonsResponse: MessageFns<ListPersonsResponse> = {
  encode(message: ListPersonsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.results) {
      PersonResponse.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.total !== "0") {
      writer.uint32(16).int64(message.total);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListPersonsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListPersonsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.results.push(PersonResponse.decode(reader, reader.uint32()));
          continue;
        }
        case 2: {
          if (tag !== 16) {
            break;
          }

          message.total = reader.int64().toString();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListPersonsResponse {
    return {
      results: globalThis.Array.isArray(object?.results)
        ? object.results.map((e: any) => PersonResponse.fromJSON(e))
        : [],
      total: isSet(object.total) ? globalThis.String(object.total) : "0",
    };
  },

  toJSON(message: ListPersonsResponse): unknown {
    const obj: any = {};
    if (message.results?.length) {
      obj.results = message.results.map((e) => PersonResponse.toJSON(e));
    }
    if (message.total !== "0") {
      obj.total = message.total;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListPersonsResponse>, I>>(base?: I): ListPersonsResponse {
    return ListPersonsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListPersonsResponse>, I>>(object: I): ListPersonsResponse {
    const message = createBaseListPersonsResponse();
    message.results = object.results?.map((e) => PersonResponse.fromPartial(e)) || [];
    message.total = object.total ?? "0";
    return message;
  },
};

function createBaseGetPersonRequest(): GetPersonRequest {
  return { projectId: "", distinctId: "" };
}

export const GetPersonRequest: MessageFns<GetPersonRequest> = {
  encode(message: GetPersonRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.projectId !== "") {
      writer.uint32(10).string(message.projectId);
    }
    if (message.distinctId !== "") {
      writer.uint32(18).string(message.distinctId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPersonRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPersonRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.projectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.distinctId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPersonRequest {
    return {
      projectId: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
      distinctId: isSet(object.distinctId)
        ? globalThis.String(object.distinctId)
        : isSet(object.distinct_id)
        ? globalThis.String(object.distinct_id)
        : "",
    };
  },

  toJSON(message: GetPersonRequest): unknown {
    const obj: any = {};
    if (message.projectId !== "") {
      obj.projectId = message.projectId;
    }
    if (message.distinctId !== "") {
      obj.distinctId = message.distinctId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPersonRequest>, I>>(base?: I): GetPersonRequest {
    return GetPersonRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPersonRequest>, I>>(object: I): GetPersonRequest {
    const message = createBaseGetPersonRequest();
    message.projectId = object.projectId ?? "";
    message.distinctId = object.distinctId ?? "";
    return message;
  },
};

function createBaseGetPersonResponse(): GetPersonResponse {
  return { person: undefined, recentEvents: [] };
}

export const GetPersonResponse: MessageFns<GetPersonResponse> = {
  encode(message: GetPersonResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.person !== undefined) {
      PersonResponse.encode(message.person, writer.uint32(10).fork()).join();
    }
    for (const v of message.recentEvents) {
      EventResult.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetPersonResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetPersonResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.person = PersonResponse.decode(reader, reader.uint32());
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.recentEvents.push(EventResult.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetPersonResponse {
    return {
      person: isSet(object.person) ? PersonResponse.fromJSON(object.person) : undefined,
      recentEvents: globalThis.Array.isArray(object?.recentEvents)
        ? object.recentEvents.map((e: any) => EventResult.fromJSON(e))
        : globalThis.Array.isArray(object?.recent_events)
        ? object.recent_events.map((e: any) => EventResult.fromJSON(e))
        : [],
    };
  },

  toJSON(message: GetPersonResponse): unknown {
    const obj: any = {};
    if (message.person !== undefined) {
      obj.person = PersonResponse.toJSON(message.person);
    }
    if (message.recentEvents?.length) {
      obj.recentEvents = message.recentEvents.map((e) => EventResult.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetPersonResponse>, I>>(base?: I): GetPersonResponse {
    return GetPersonResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetPersonResponse>, I>>(object: I): GetPersonResponse {
    const message = createBaseGetPersonResponse();
    message.person = (object.person !== undefined && object.person !== null)
      ? PersonResponse.fromPartial(object.person)
      : undefined;
    message.recentEvents = object.recentEvents?.map((e) => EventResult.fromPartial(e)) || [];
    return message;
  },
};

function createBaseDeletePersonRequest(): DeletePersonRequest {
  return { projectId: "", distinctId: "" };
}

export const DeletePersonRequest: MessageFns<DeletePersonRequest> = {
  encode(message: DeletePersonRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.projectId !== "") {
      writer.uint32(10).string(message.projectId);
    }
    if (message.distinctId !== "") {
      writer.uint32(18).string(message.distinctId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeletePersonRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeletePersonRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.projectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.distinctId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeletePersonRequest {
    return {
      projectId: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
      distinctId: isSet(object.distinctId)
        ? globalThis.String(object.distinctId)
        : isSet(object.distinct_id)
        ? globalThis.String(object.distinct_id)
        : "",
    };
  },

  toJSON(message: DeletePersonRequest): unknown {
    const obj: any = {};
    if (message.projectId !== "") {
      obj.projectId = message.projectId;
    }
    if (message.distinctId !== "") {
      obj.distinctId = message.distinctId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeletePersonRequest>, I>>(base?: I): DeletePersonRequest {
    return DeletePersonRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeletePersonRequest>, I>>(object: I): DeletePersonRequest {
    const message = createBaseDeletePersonRequest();
    message.projectId = object.projectId ?? "";
    message.distinctId = object.distinctId ?? "";
    return message;
  },
};

function createBaseDeletePersonResponse(): DeletePersonResponse {
  return {};
}

export const DeletePersonResponse: MessageFns<DeletePersonResponse> = {
  encode(_: DeletePersonResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeletePersonResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeletePersonResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): DeletePersonResponse {
    return {};
  },

  toJSON(_: DeletePersonResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<DeletePersonResponse>, I>>(base?: I): DeletePersonResponse {
    return DeletePersonResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeletePersonResponse>, I>>(_: I): DeletePersonResponse {
    const message = createBaseDeletePersonResponse();
    return message;
  },
};

function createBaseCohortResponse(): CohortResponse {
  return { id: "", name: "", filters: undefined, personCount: "0", createdAt: undefined };
}

export const CohortResponse: MessageFns<CohortResponse> = {
  encode(message: CohortResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.id !== "") {
      writer.uint32(10).string(message.id);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.filters !== undefined) {
      Struct.encode(Struct.wrap(message.filters), writer.uint32(26).fork()).join();
    }
    if (message.personCount !== "0") {
      writer.uint32(32).int64(message.personCount);
    }
    if (message.createdAt !== undefined) {
      Timestamp.encode(toTimestamp(message.createdAt), writer.uint32(42).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CohortResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCohortResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.id = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.filters = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.personCount = reader.int64().toString();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.createdAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CohortResponse {
    return {
      id: isSet(object.id) ? globalThis.String(object.id) : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      filters: isObject(object.filters) ? object.filters : undefined,
      personCount: isSet(object.personCount)
        ? globalThis.String(object.personCount)
        : isSet(object.person_count)
        ? globalThis.String(object.person_count)
        : "0",
      createdAt: isSet(object.createdAt)
        ? fromJsonTimestamp(object.createdAt)
        : isSet(object.created_at)
        ? fromJsonTimestamp(object.created_at)
        : undefined,
    };
  },

  toJSON(message: CohortResponse): unknown {
    const obj: any = {};
    if (message.id !== "") {
      obj.id = message.id;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.filters !== undefined) {
      obj.filters = message.filters;
    }
    if (message.personCount !== "0") {
      obj.personCount = message.personCount;
    }
    if (message.createdAt !== undefined) {
      obj.createdAt = message.createdAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CohortResponse>, I>>(base?: I): CohortResponse {
    return CohortResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CohortResponse>, I>>(object: I): CohortResponse {
    const message = createBaseCohortResponse();
    message.id = object.id ?? "";
    message.name = object.name ?? "";
    message.filters = object.filters ?? undefined;
    message.personCount = object.personCount ?? "0";
    message.createdAt = object.createdAt ?? undefined;
    return message;
  },
};

function createBaseListCohortsRequest(): ListCohortsRequest {
  return { projectId: "" };
}

export const ListCohortsRequest: MessageFns<ListCohortsRequest> = {
  encode(message: ListCohortsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.projectId !== "") {
      writer.uint32(10).string(message.projectId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListCohortsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCohortsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.projectId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListCohortsRequest {
    return {
      projectId: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
    };
  },

  toJSON(message: ListCohortsRequest): unknown {
    const obj: any = {};
    if (message.projectId !== "") {
      obj.projectId = message.projectId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListCohortsRequest>, I>>(base?: I): ListCohortsRequest {
    return ListCohortsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListCohortsRequest>, I>>(object: I): ListCohortsRequest {
    const message = createBaseListCohortsRequest();
    message.projectId = object.projectId ?? "";
    return message;
  },
};

function createBaseListCohortsResponse(): ListCohortsResponse {
  return { results: [] };
}

export const ListCohortsResponse: MessageFns<ListCohortsResponse> = {
  encode(message: ListCohortsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.results) {
      CohortResponse.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ListCohortsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseListCohortsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.results.push(CohortResponse.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ListCohortsResponse {
    return {
      results: globalThis.Array.isArray(object?.results)
        ? object.results.map((e: any) => CohortResponse.fromJSON(e))
        : [],
    };
  },

  toJSON(message: ListCohortsResponse): unknown {
    const obj: any = {};
    if (message.results?.length) {
      obj.results = message.results.map((e) => CohortResponse.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ListCohortsResponse>, I>>(base?: I): ListCohortsResponse {
    return ListCohortsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ListCohortsResponse>, I>>(object: I): ListCohortsResponse {
    const message = createBaseListCohortsResponse();
    message.results = object.results?.map((e) => CohortResponse.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCreateCohortRequest(): CreateCohortRequest {
  return { projectId: "", name: "", filters: undefined };
}

export const CreateCohortRequest: MessageFns<CreateCohortRequest> = {
  encode(message: CreateCohortRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.projectId !== "") {
      writer.uint32(10).string(message.projectId);
    }
    if (message.name !== "") {
      writer.uint32(18).string(message.name);
    }
    if (message.filters !== undefined) {
      Struct.encode(Struct.wrap(message.filters), writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateCohortRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateCohortRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.projectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.name = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.filters = Struct.unwrap(Struct.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateCohortRequest {
    return {
      projectId: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
      name: isSet(object.name) ? globalThis.String(object.name) : "",
      filters: isObject(object.filters) ? object.filters : undefined,
    };
  },

  toJSON(message: CreateCohortRequest): unknown {
    const obj: any = {};
    if (message.projectId !== "") {
      obj.projectId = message.projectId;
    }
    if (message.name !== "") {
      obj.name = message.name;
    }
    if (message.filters !== undefined) {
      obj.filters = message.filters;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateCohortRequest>, I>>(base?: I): CreateCohortRequest {
    return CreateCohortRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateCohortRequest>, I>>(object: I): CreateCohortRequest {
    const message = createBaseCreateCohortRequest();
    message.projectId = object.projectId ?? "";
    message.name = object.name ?? "";
    message.filters = object.filters ?? undefined;
    return message;
  },
};

function createBaseDeleteCohortRequest(): DeleteCohortRequest {
  return { projectId: "", cohortId: "" };
}

export const DeleteCohortRequest: MessageFns<DeleteCohortRequest> = {
  encode(message: DeleteCohortRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.projectId !== "") {
      writer.uint32(10).string(message.projectId);
    }
    if (message.cohortId !== "") {
      writer.uint32(18).string(message.cohortId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteCohortRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteCohortRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.projectId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.cohortId = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): DeleteCohortRequest {
    return {
      projectId: isSet(object.projectId)
        ? globalThis.String(object.projectId)
        : isSet(object.project_id)
        ? globalThis.String(object.project_id)
        : "",
      cohortId: isSet(object.cohortId)
        ? globalThis.String(object.cohortId)
        : isSet(object.cohort_id)
        ? globalThis.String(object.cohort_id)
        : "",
    };
  },

  toJSON(message: DeleteCohortRequest): unknown {
    const obj: any = {};
    if (message.projectId !== "") {
      obj.projectId = message.projectId;
    }
    if (message.cohortId !== "") {
      obj.cohortId = message.cohortId;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteCohortRequest>, I>>(base?: I): DeleteCohortRequest {
    return DeleteCohortRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteCohortRequest>, I>>(object: I): DeleteCohortRequest {
    const message = createBaseDeleteCohortRequest();
    message.projectId = object.projectId ?? "";
    message.cohortId = object.cohortId ?? "";
    return message;
  },
};

function createBaseDeleteCohortResponse(): DeleteCohortResponse {
  return {};
}

export const DeleteCohortResponse: MessageFns<DeleteCohortResponse> = {
  encode(_: DeleteCohortResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): DeleteCohortResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseDeleteCohortResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(_: any): DeleteCohortResponse {
    return {};
  },

  toJSON(_: DeleteCohortResponse): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<DeleteCohortResponse>, I>>(base?: I): DeleteCohortResponse {
    return DeleteCohortResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<DeleteCohortResponse>, I>>(_: I): DeleteCohortResponse {
    const message = createBaseDeleteCohortResponse();
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000).toString();
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (globalThis.Number(t.seconds) || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
